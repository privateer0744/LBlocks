# LBlocks
 Modularization tool using blocks with input and output

## Install
```
mkdir build
cd build
cmake .. -DCMAKE_INSTALL_PREFIX=<Target Path>
cmake --install .
```

## Build test
```
cmake .. -DBUILD_TEST=1
cmake --build .
```